#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    srand(time(NULL));
    int rando;

    for (; ;) {
        rando = rand() % 6 + 1;
        if (rando == 6) {
            printf("You won! You draw %d!\n", random);
            break;
        }
        else {
            printf("%d\n", rando);
        }
    }

    return 0;
}
